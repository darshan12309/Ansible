-
    name: 'test
    hosts: web_nodes
    roles:
        - role : trask-A
          when: 'ansible_host=="server4.company.com"'

===============================================================================

-
    name: 'test
    hosts: web_nodes
    roles:
        - role : trask-A
          when: 'ansible_host=="server4.company.com"'
    dependencies:
        - { role: tire }
            